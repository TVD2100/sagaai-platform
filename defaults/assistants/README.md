# Default assistants

Each subfolder represents one default assistant:
- `manifest.json` - model, temperature, tools, max_tool_calls, max_tokens
- `prompt.md` - system prompt text
- `files/` - optional attachment files

Delete a subfolder to exclude the assistant from default import.

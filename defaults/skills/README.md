# Default standardized skills

Put skills here as subfolders. Each skill needs a `SKILL.md` (or `AGENT_SYSTEM_PROMPT.md`) with instructions. On first boot, if the runtime skills library is empty, these skills are registered automatically. Delete a skill subfolder to exclude it from default import.
